{
  "name": "pshell",
  "version": "1.1.0",
  "description": "Promise-based portable interface for running shell commands",
  "author": "Max Jung <max@gourmetjs.com>",
  "license": "MIT",
  "repository": "https://github.com/asyncmax/pshell",
  "main": "lib/index.js",
  "scripts": {
    "lint": "eslint lib/*.js test/**/*.js examples/*.js",
    "tape": "tape test/*.js",
    "test": "npm run lint && npm run tape"
  },
  "devDependencies": {
    "es6-promise": "^3.3.1",
    "eslint": "^3.5.0",
    "tape": "^4.6.0"
  }
}
